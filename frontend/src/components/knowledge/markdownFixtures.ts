/** Эталонные сниппеты для проверки рендера Markdown (GFM + breaks). */

export const FIXTURE_REGULAMENT = `## Глава 1. Назначение
1.1. Первый пункт.
1.2. Второй пункт.
1.3. Третий пункт.`

export const FIXTURE_NESTED_LISTS = `- пункт
- пункт
  - вложенный
  - ещё
1. первый
2. второй
   - подпункт`

export const FIXTURE_TABLE = `| Колонка | Значение |
| --- | --- |
| A | 1 |
| B | 2 |`

export const FIXTURE_TASKS = `- [ ] открыто
- [x] готово`

export const FIXTURE_UNSAFE_LINK = `[x](javascript:alert(1))`

/** Список, затем заголовок регламента без пустой строки — не должен остаться внутри <li> */
export const FIXTURE_LIST_THEN_REG = `2.4.1. Для должности лидера:
- 7 вопросов по общим правилам;
- 6 вопросов по организации;
- 3 вопроса с RP-ситуациями.
2.4.2. Для должности судьи:
- 5 вопросов по суду;`

export const FIXTURE_ALERTS = `> [!NOTE]
> Обычная заметка.

> [!WARNING]
> Нельзя одинаковые вопросы подряд.

> [!DANGER]
> Критичное ограничение.`

/** Алерт без пустой строки, затем регламент — 1.1 не должен быть внутри заметки */
export const FIXTURE_ALERT_THEN_REG = `> [!NOTE]
> Короткая заметка.
1.1. Заявки на пост лидера.
1.2. Второй пункт.`


